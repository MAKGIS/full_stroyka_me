export interface BlockHeaderGroup {
    name: string;
    current: boolean;
}

export interface BlockSlide {
    title: string;
    text: string;
    image_classic?: string;
    image_full?: string;
    image_mobile?: string;
}
